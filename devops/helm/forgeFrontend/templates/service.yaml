apiVersion: v1
kind: Service
metadata:
  name: {{ include "forgeFrontend.fullname" . }}
  labels:
    {{- include "forgeFrontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "forgeFrontend.selectorLabels" . | nindent 4 }}
