apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "forgeBackend.fullname" . }}-test-connection"
  labels:
    {{- include "forgeBackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "forgeBackend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
