parameters:
  - name: 'jobName'
    type: string
  - name: 'serviceConnection' #Kubernetes service connection name
    type: string
  - name: 'manifestPath' #Kubernetes manifest file path
    type: string
jobs:
  - job: ${{ parameters.jobName }}
    steps:
      - task: KubernetesManifest@0
        inputs:
          action: 'deploy'
          kubernetesServiceConnection: '${{ parameters.serviceConnection }}'
          manifests: '${{ parameters.manifestPath }}'
          rolloutStatusTimeout: '10'
